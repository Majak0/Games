{
    "$schema": "https://www.schemastore.org/package.json",
    "private": true,
    "type": "module",
    "scripts": {
        "build": "vite build",
        "dev": "vite",
        "typecheck": "tsc --noEmit",
        "audit:assets": "node scripts/audit-country-assets.mjs",
        "fix:assets": "node scripts/fix-missing-assets.mjs",
        "generate:shapes": "node scripts/generate-missing-shapes.mjs",
        "generate:flags": "node scripts/generate-missing-flags.mjs --defaults"
    },
    "devDependencies": {
        "@tailwindcss/vite": "^4.0.0",
        "@types/node": "^26.0.0",
        "concurrently": "^9.0.1",
        "laravel-vite-plugin": "^3.1",
        "tailwindcss": "^4.0.0",
        "typescript": "^6.0.3",
        "vite": "^8.0.0"
    },
    "dependencies": {
        "@svg-maps/world": "^2.0.0",
        "bootstrap-icons": "^1.13.1"
    }
}
